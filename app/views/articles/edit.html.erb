
<h1>Edit Article</h1>
 
 <%# We are passing the @article object to the from rails_helper thing. This tells it this is an UPDATE/PATCH and not a CREATE %>
<%= render 'form' %>
 
<%= link_to 'Back', articles_path %>